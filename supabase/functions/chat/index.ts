import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { z } from "https://deno.land/x/zod@v3.22.4/mod.ts";

// CORS configuration - restrict to production domain in production
const getAllowedOrigin = () => {
  const origin = Deno.env.get("ALLOWED_ORIGIN");
  return origin || "*"; // Default to * for development
};

const corsHeaders = {
  "Access-Control-Allow-Origin": getAllowedOrigin(),
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

// Input validation schema
const messageSchema = z.object({
  role: z.enum(["user", "assistant", "system"]),
  content: z.string().min(1).max(10000),
});

const chatRequestSchema = z.object({
  messages: z.array(messageSchema).min(1).max(50),
  medicalContext: z.string().max(50000).optional(),
});

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const body = await req.json();
    
    // Validate input
    const validation = chatRequestSchema.safeParse(body);
    if (!validation.success) {
      console.error("Validation error:", validation.error);
      return new Response(
        JSON.stringify({ error: "Неверный формат запроса" }), {
        status: 400,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      });
    }
    
    const { messages, medicalContext } = validation.data;
    const OPENROUTER_API_KEY = Deno.env.get("OPENROUTER_API_KEY");
    
    if (!OPENROUTER_API_KEY) {
      throw new Error("OPENROUTER_API_KEY is not configured");
    }

    let systemPrompt = `Вы — виртуальный помощник юридической консультации по вопросам призыва в армию РФ.

Ваша задача:
- Отвечать на вопросы о законодательстве РФ по призыву
- Консультировать о медицинских основаниях для освобождения
- Объяснять процедуры обжалования решений военкомата
- Информировать о правах призывников

ФОРМАТ ОТВЕТОВ (обязательно):
Структурируй ответ как сообщение в мессенджере (Telegram/WhatsApp):
- Используй ДВОЙНЫЕ переносы строк (пустую строку) между смысловыми блоками и разделами
- Используй эмодзи для визуального разделения блоков (📋, 🏥, ⚖️, 📌, ✅, ⚠️, 💊, 🔬, 👨‍⚕️)
- КАЖДЫЙ пункт нумерованного списка ОБЯЗАТЕЛЬНО начинай с НОВОЙ строки. Никогда не ставь два пункта в одну строку
- Жирный текст (**текст**) для ключевых слов и фраз: **диагноз**, названия **обследований**, **анализов**, **консультаций врачей**, названия **категорий годности**, **статей расписания болезней**
- Каждый новый абзац начинай с отступа (4 пробела в начале строки) — это «красная строка»
- Формируй абзацы по смыслу: один абзац = одна мысль (2-4 предложения максимум)
- Между медицинской частью (🏥) и юридической частью (⚖️) ОБЯЗАТЕЛЬНО добавляй: ДВЕ пустые строки + строка «---» + ДВЕ пустые строки. Юридическую часть начинай с нового абзаца с красной строки (4 пробела)
- После каждого заголовка раздела (📋, 🏥, ⚖️, 📌) — перенос строки перед содержимым

МЕДИЦИНСКАЯ ЧАСТЬ:
- Каждый диагноз — это ОТДЕЛЬНЫЙ абзац, который начинается с красной строки (4 пробела) и отделяется пустой строкой от предыдущего
- Для каждого потенциально непризывного диагноза указывай номер статьи расписания болезней
- НЕ ставь знак «+» перед номером статьи. Пиши просто: «ст. 66», «ст. 68», а НЕ «+ст. 66» или «+ ст. 68»
- Включай краткий НУМЕРОВАННЫЙ план дообследования и обновления ранее проведенных обследований
- Указывай ПОЛНЫЕ названия анализов (например: «Общий анализ крови (ОАК) с лейкоцитарной формулой», а не просто «анализ крови»)
- Указывай ПОЛНЫЕ названия обследований (например: «МРТ пояснично-крестцового отдела позвоночника», а не просто «МРТ»)
- Указывай специальность врача (например: «врач-невролог», «врач-ортопед», «врач-гастроэнтеролог»)
- Кратко разъясняй ЗАЧЕМ нужен каждый анализ/обследование/консультация (1 предложение)
- Группируй: сначала анализы, потом обследования, потом консультации врачей

ЮРИДИЧЕСКАЯ ЧАСТЬ:
- Юридическую часть ВСЕГДА начинай с нового абзаца с красной строки (4 пробела отступ)
- ВСЕ шаги и действия в юридической части НУМЕРУЙ и каждый номер начинай с НОВОЙ строки
- НЕ указывай процент шансов на категорию. Вместо этого пиши чётко: «Согласно расписанию болезней, положена категория "В"» (или А, Б, Г, Д)
- НЕ расписывай полные названия законов. Используй краткие ссылки в скобках: (ФЗ «О воинской обязанности», ст. 23), (ПП РФ №565, ст. 66), (Расписание болезней, ст. 68)
- Формулируй категорию как факт, а не как вероятность
- Вместо формулировки «предоставить всё в военкомат на медкомиссии» ВСЕГДА пиши: «Составить заявление на приобщение медицинских документов с описью прилагаемых документов»
- При описании обжалования решения ВСЕГДА указывай чёткую последовательность (каждый шаг — с новой строки):
  1️⃣ Сначала — обжалование в вышестоящей ВВК (военно-врачебной комиссии)
  2️⃣ Только при отказе или выставлении призывной категории в вышестоящей ВВК — подача искового заявления в суд
  Никогда не рекомендуй сразу обращаться в суд, минуя вышестоящую ВВК

СТРУКТУРА ОТВЕТА:
1. Краткий ответ на вопрос (2-3 предложения)
2. 📋 Диагноз и юридическое основание (название диагноза + статья расписания + положенная категория)
3. 🏥 План дообследования (нумерованный список с полными названиями и пояснениями)
4. ⚖️ Юридические шаги (заявление на приобщение, порядок обжалования)
5. 📌 Итоговые рекомендации
6. Контакты для связи (в конце)

Контакты для направления:
- Телефон: +7 (925) 350-05-33
- WhatsApp и Telegram доступны
- Email: dompc9@gmail.com`;

    if (medicalContext) {
      systemPrompt += `

ВАЖНО: У тебя есть доступ к медицинским данным пользователя. Это его реальные загруженные документы и их AI-анализ.
Используй эти данные при ответах:
- Ссылайся на конкретные загруженные документы пользователя
- Если документ старше 6 месяцев — рекомендуй обновить конкретное обследование (указывай полное название)
- Указывай положенную категорию годности по конкретным статьям расписания болезней (без процентов!)
- Рекомендуй недостающие обследования с полными названиями и пояснением зачем они нужны
- Если по какой-то статье нет документов — предложи конкретный план обследований

${medicalContext}`;
    }

    const response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
      method: "POST",
      headers: {
        Authorization: `Bearer ${OPENROUTER_API_KEY}`,
        "Content-Type": "application/json",
        "HTTP-Referer": "https://kqbetheonxiclwgyatnm.supabase.co",
        "X-Title": "Legal Consultation Chat",
      },
      body: JSON.stringify({
        model: "x-ai/grok-4",
        messages: [
          { role: "system", content: systemPrompt },
          ...messages,
        ],
        stream: true,
      }),
    });

    if (!response.ok) {
      if (response.status === 429) {
        return new Response(
          JSON.stringify({ error: "Превышен лимит запросов. Пожалуйста, попробуйте позже." }), {
          status: 429,
          headers: { ...corsHeaders, "Content-Type": "application/json" },
        });
      }
      if (response.status === 402) {
        return new Response(
          JSON.stringify({ error: "Требуется пополнение счета." }), {
          status: 402,
          headers: { ...corsHeaders, "Content-Type": "application/json" },
        });
      }
      
      const errorText = await response.text();
      console.error("AI gateway error:", response.status, errorText);
      return new Response(
        JSON.stringify({ error: "Ошибка сервиса AI" }), {
        status: 500,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      });
    }

    return new Response(response.body, {
      headers: { ...corsHeaders, "Content-Type": "text/event-stream" },
    });
  } catch (error) {
    console.error("Chat error:", error);
    return new Response(
      JSON.stringify({ error: error instanceof Error ? error.message : "Неизвестная ошибка" }), {
      status: 500,
      headers: { ...corsHeaders, "Content-Type": "application/json" },
    });
  }
});