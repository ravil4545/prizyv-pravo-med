-- ════════════════════════════════════════════════════════════════════════
--  Client-initiated связь: индивидуальные коды приглашений упразднены.
--
--  Связь юрист↔клиент теперь инициирует ТОЛЬКО клиент (каталог /lawyers или
--  ссылка на профиль юриста → тумблер доступа в его кабинете). Поэтому больше
--  НЕ авто-генерим invite_code при создании карточки клиента.
--
--  Неразрушающе: RPC-функции кодов (claim_lawyer_invite / regenerate_lawyer_invite
--  / generate_lawyer_invite_code) и существующие значения invite_code остаются
--  в БД как legacy — UI их больше не вызывает. Дропаем только триггер автогенерации.
-- ════════════════════════════════════════════════════════════════════════

DROP TRIGGER IF EXISTS trg_lawyer_clients_invite_code ON public.lawyer_clients;

NOTIFY pgrst, 'reload schema';
