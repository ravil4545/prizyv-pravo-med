# SPA-fallback для Netlify: любой неизвестный путь отдаётся как /index.html
# с HTTP 200, чтобы React Router на клиенте обработал роутинг
# (без этого правила прямой заход на /u/<slug>, /blog/<slug>, /dashboard
# и любые другие SPA-маршруты возвращает 404 от Netlify).
#
# Должно быть последним правилом — все более специфичные редиректы
# можно добавлять выше этой строки.

/*    /index.html   200
